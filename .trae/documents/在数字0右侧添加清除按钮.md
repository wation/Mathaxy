## 实现计划

### 1. 分析当前界面结构
- 游戏界面在 `GamePlayView.swift` 中实现
- 答案网格使用 `Grid` 布局，共4行3列
- 第4行当前布局：[清空按钮, 0, 占位符]

### 2. 实现方案
- 将第4行的占位符替换为新的清除按钮
- 新按钮调用与现有清空按钮相同的 `viewModel.clearAllInput()` 方法
- 保持与现有清空按钮相同的外观样式

### 3. 代码修改
1. 打开 `GamePlayView.swift` 文件
2. 找到 `answerGrid` 计算属性（第208-244行）
3. 修改第4行的 `GridRow` 内容：
   - 删除占位符 `Color.clear`
   - 添加新的 `ClearAllButton` 实例

### 4. 预期效果
- 第4行布局变为：[清空按钮, 0, 清除按钮]
- 两个清除按钮功能完全相同
- 保持界面布局的对称性和美观性

### 5. 测试验证
- 运行游戏，检查界面布局是否正确
- 点击新添加的清除按钮，验证是否能清空用户输入

### 6. 技术要点
- 使用现有的 `ClearAllButton` 组件，无需创建新组件
- 复用现有的 `clearAllInput()` 方法，确保功能一致性
- 保持代码简洁，遵循DRY原则